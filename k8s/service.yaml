apiVersion: v1
kind: Service

metadata:
  name: bella-service

spec:
  type: NodePort

  selector:
    app: bella

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007